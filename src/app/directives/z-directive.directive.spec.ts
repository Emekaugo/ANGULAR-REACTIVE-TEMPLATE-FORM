import { ZDirectiveDirective } from './z-directive.directive';

describe('ZDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ZDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
